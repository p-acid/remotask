"""Agent runtime package — production worker entry points.

For 003 (e2e-demo) this only exports ``demo_worker``, the placeholder workload
the daemon spawns when a session moves to ``running``. A real claude-agent-sdk
driver will live alongside it in a future feature.
"""
